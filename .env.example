# Copy this file to .env.local and fill in the real value.
# .env.local is gitignored — it will never be committed.
#
# In production (Vercel), this is set under:
# Project Settings → Environments → Production → Add Environment Variable

HUBSPOT_TOKEN=
